# テンジポート｜デモ紹介LP・設計実装仕様書

作成日：2026-09-12／改訂：2026-09-13／版：1.1
関連方針：`tenjiport_email_sales_strategy.md`

### 1.1版での変更点（2026-09-13）

- 4章の操作デモ方式を確定：静的モック案ではなく「実環境シード方式」（本物の画面・DBをそのままデモ専用組織に対して使い、送信・課金だけをガードする）を採用。理由は本体アプリが完成済みとなり、UIを二重メンテするコストが実装コストを上回ると判断したため。詳細は4.3節。
- 料金（開催ごと9,800円/30社・31社目から300円、年間298,000円/6開催・300社）はいずれも`docs/open-decisions.md`で確定済み・本番投入済みであることを確認。3.6節・7章・11章の「未確定」表現を実状に合わせて更新。
- ドメイン（tenjiport.com）・特定商取引法ページ（`/legal/tokushoho`）も確定済みのため11章から除外。
- 10章に、受け入れ条件の自動検証手段としてPlaywright導入を追記。

## 0. Claude Codeへの実装指示

この仕様に基づき、既存プロジェクトにデモ紹介LPを実装する。まずAGENTS.md等の指示、ルーティング、デザイン、ロゴ、実機能、認証、分析基盤を確認する。既存の技術・コンポーネントを優先し、フレームワーク移行を行わない。

今回の文書は実装依頼用であり、実装済みの証明ではない。情報不足でもページ・状態・仮データ・設定口は先に作り、公開前の要確定事項を最後に一覧化する。予約URLや利用開始URL等を推測して実在するように作らない。

本書は公開用営業LPとサンプルデモの仕様。実顧客の管理画面、実配信、実決済は別環境とする。

## 1. 目的・対象・導線

対象は営業メールを受け取った展示会主催者・運営事務局。主催者が3分程度で「誰が未提出・未確認かを把握できる」「出展者も簡単に使える」を理解できるページにする。

主な行動：登録不要の操作デモを試す。
副次的な行動：資料閲覧・PDF取得、15分の導入相談予約。
事業上の到達点：実サービスの利用開始、有料導入。

LP提案パス：`/demo`。操作画面：`/demo/app`。既存ルートと衝突する場合は調整し、営業リンク・計測を一貫させる。登録・メール入力をデモ閲覧の条件にしない。

## 2. メッセージと表現の原則

主訴求：「出展者情報も、資料の確認状況も。ひとつの画面で。」
補足：「情報収集、資料配布、確認状況、銀行振込の入金確認をまとめて管理。主催者と出展者、両方の画面を体験できます。」

避ける表現：架空の導入実績、効果の断定、未取得認証、根拠のないNo.1、実現していない自動入金消込、商標登録済み表示、無料で本番運用できるという誤認。

「確認済み」は出展者の明示操作、「入金確認済み」は主催者の手動操作として一貫させる。メール開封・ファイル閲覧を確認済みに自動変換しない。

## 3. ページ構成・表示文言

### 3.1 ヘッダー

左：採用済み01ロゴ＋必要ならテキストでサービス名。右：体験内容、料金、資料、導入相談のページ内リンク。
PCは主CTA「デモを試す」。モバイルはナビを簡潔にし、固定CTAが本文や予約画面を隠さない。

### 3.2 ファーストビュー

- 補助見出し：「展示会・出展者管理クラウド」
- H1：「出展者情報も、資料の確認状況も。ひとつの画面で。」
- 本文：本書2節の補足文。
- 主ボタン：「登録不要でデモを試す」→操作デモへ。
- 補助表示：「架空の展示会データで体験できます。実際のメール送信や課金は行われません。」
- 小さな副リンク：「サービス資料を見る」「15分の導入相談を予約」。
- 右または下：実際のデモ画面をキャプチャした管理一覧。日本語ラベルと状態が読み取れる大きさ。

動画・画面画像にプレイボタンを置くなら、実際に再生できること。動画未制作時は静止画にして偽のプレイボタンを出さない。

### 3.3 課題を3点だけ提示

見出し：「こんな確認作業に、時間を取られていませんか？」

1. メールとExcelを照合し、未提出の出展者を探す。
2. 配布した搬入案内が確認されたか、個別に連絡する。
3. 請求書と入金確認を、別々の表で管理する。

大げさな不安訴求を避け、各課題に対応する実画面へつなぐ。

### 3.4 体験内容・デモ入口

見出し：「主催者と出展者の操作を、ひと通り体験」
体験の順序：一覧を見る → 案内を公開 → 出展者が確認 → 一覧に反映。

短い動画が用意できれば60〜90秒、字幕付き、操作で再生、音声自動再生なし。横にまたは直後にデモへの主CTA。動画視聴をデモの前提にしない。

紹介LP自体に重い管理画面を常時読み込まず、別ルートのデモを遷移時に読み込む。PC・モバイルとも利用でき、元のLPの資料・予約に戻れるリンクを常設。

### 3.5 導入の流れ

見出し：「最初の情報収集から、そのまま管理へ」

1. 主催者が入力項目とイベントを設定。
2. 出展者へ登録URLを案内。
3. 集まった情報と配布資料の確認状況を管理。

「会議なしでも利用開始できます」はセルフサービスで開始できることを実装確認した場合のみ掲載。
継続利用機能が実装済みなら、下に「次回は前回の設定・情報を活用」を追加。未実装なら現行機能として掲載しない。

### 3.6 料金と見積もり目安

開催ごと：9,800円（税込）／1開催・30社まで。31社目から1社300円（税込）。`docs/open-decisions.md`で税込表記が確定済み（1.0版時点の「税別想定」から訂正）。
年間：6開催・各開催300社まで。298,000円で確定・本番投入済み（税表記は開催ごとプランと同様に税込を基本とする）。
個別見積もり：年間7開催以上、1開催301社以上の年間利用等。主催法人・支援範囲等も確認。

任意の簡単な料金シミュレーターを実装：出展者数N、開催数Eを入力。都度合計=E×(9800+max(0,N-30)×300)。整数・正数のみ許容。年契約比較はE<=6かつN<=300の場合に表示する。各開催の社数が同じ場合の概算と明示する。

検算用：30社=9,800円、31社=10,100円、100社=30,800円、200社=60,800円、300社=90,800円。300社×6回=544,800円。

「実請求額が確定」「決済が完了」等をシミュレーターに表示しない。

料金・年額とも`docs/open-decisions.md`で確定済み・本番投入済み（`pricing_configs`・`annual_plan_configs`に`is_test=false`で反映済み）のため、本番表示として公開してよい。今後価格改定時はここと本番設定の両方を同時に更新すること。

### 3.7 資料

見出し：「社内でのご検討・共有に」
本文：「機能、導入の流れ、料金をまとめたサービス資料をご覧いただけます。」
ボタン：「資料を読む」「PDFをダウンロード」。フォーム・メール登録不要。

資料は5ページ程度を想定：課題、運用フロー、主な画面、料金と導入手順、運営会社と支援範囲。これは別制作する営業PDFの構成であり、本仕様書を営業PDFとして配布しない。

実PDFがある場合にのみダウンロードを有効化し、改訂日・サイズを実値で表示。未提供の開発環境は準備中表示とし、本番でダミーリンクを残さない。スマホでは外部PDFビューアでも開けるリンクを用意する。

### 3.8 FAQ

以下の問いに、確認できた範囲で短く回答する。

- Q. デモには登録が必要ですか？ A. 不要です。架空データを使った操作体験です。
- Q. デモからメールが送信されたり課金されたりしますか？ A. ありません。通知は画面内で再現します。
- Q. 出展者はどこから使い始めますか？ A. 主催者から案内されたURLで情報を入力します。以後の本人確認方法は実仕様と一致させます。
- Q. 銀行振込の入金も自動で確認されますか？ A. 主催者が銀行への入金を確認し、手動で状態を更新します。
- Q. Excelで使えますか？ A. 実装済みのCSVまたはExcel出力形式を正確に説明します。CSVのみなら「Excelファイル」と表現しません。
- Q. すでに募集が始まっていても利用できますか？ A. 現在の収集状況によって導入方法が変わるため、導入相談を案内。対応未確認の自動移行を約束しません。
- Q. 会議は必須ですか？ A. 実サービスが自己登録可能なら必須ではないと案内。そうでない開発段階では現状を明示します。
- Q. 年間プランの上限を超える場合は？ A. 個別見積もりへ案内。

### 3.9 導入相談・外部予約

見出し：「自社の運用に合うか、15分で相談」
本文：「現在のPDF・Excel運用、次回開催までの準備、開催規模に応じた料金をご相談いただけます。」

相談内容を3つだけ表示し、「空き日時を確認する」で外部予約を開始。メールで質問できる代替連絡先も置く。
予約UIとデモUIを同じ画面上で競合させない。ポップアップで予約を強制しない。

### 3.10 最終CTA・フッター

見出し：「まずは、次の開催で使うイメージを。」
主CTA：「登録不要でデモを試す」。副CTA：「利用開始へ」（実導線が利用可能な場合）。相談・資料にも戻れる。
フッター：運営法人、問い合わせ、プライバシーポリシー、利用規約、必要な販売条件へのリンク。法人名・連絡先は公開承認された値のみ使う。

## 4. 操作デモの詳細

### 4.1 初期データとガイド

架空イベント「テンジポート サンプル展示会」。架空の出展者12社を用意。初期状態は情報提出済み9社・未提出3社、搬入案内確認済み7社・未確認5社、入金確認済み8社・未確認4社。これらは独立した状態で、画面上の集計はデータから計算する。

実在会社名、電話番号、実メールアドレスをサンプルに使わない。送信不能なexample.comのアドレスを使用。請求額も架空と明示する。

常設表示：「デモ環境／サンプルデータ」「最初に戻す」「資料・導入相談」。ガイドは閉じて自由操作できる。

### 4.2 主要シナリオ

1. 一覧から「未提出」を絞り込む。該当3社に変わる。
2. 用意済みサンプル資料を選び、「搬入案内・更新版」を下書き作成。対象は3社を指定。
3. 確認画面に資料名・対象3社・通知内容を表示。「公開を体験」で、その資料に対する確認状態を未確認3社にする。通知プレビューを表示し、実送信しない。
4. 対象の1社の出展者画面へ切替。資料を閲覧しただけでは確認済みにしない。「確認しました」を押す。
5. 主催者画面へ戻り、更新版の確認済み1社・未確認2社を表示。旧資料の確認履歴と混同しない。
6. 未入金の1社を選択し、「銀行への入金を確認しました」と明示した操作で入金確認済みにする。集計は9社・3社になる。
7. サンプル一覧をCSV等の実装済み形式で出力する。

完了表示：「情報収集と資料の確認状況を、同じ画面で管理できます。」
続きの行動：「自社イベントで利用を始める」「導入について相談する」。実サービス未公開なら利用開始を募集受付等の実導線に置き換える。架空の成功ページに送らない。

### 4.3 分離と制限（実環境シード方式・確定）

本体アプリが完成済みであることを踏まえ、静的モックではなく**実環境シード方式**を採用する。実アプリの画面・DB・サーバーアクションをそのまま使い、デモ専用に用意した組織（テナント）に対してのみ動作させる。UIを二重メンテしないことを優先し、本番機能との乖離を防ぐ。

**テナント分離**：`organizations`に`is_demo`フラグを追加し、デモ専用組織1件・イベント1件・出展者12社を4.1節の状態配分でシードする。デモ専用の`seed`スクリプト（再シード可能な形）で管理する。

**認証はSupabase Authを経由しない**：主催者側のマジックリンク不具合（`docs/handoff.md`記載、カスタムSMTP対応待ち）に依存させないため、`/demo/app`アクセス時にサーバーアクションで署名付きデモ専用Cookieを発行し、demo org idにスコープしたservice-roleクエリで読み書きする。実顧客の認証経路（Supabase Auth・RLS）には一切触れない。出展者側は実際の`apply/[token]`フローをデモ用の固定トークンでそのまま体験させる。

**送信・課金のガードレール**：Resend送信（`src/lib/billing/notifyOrganizer.ts`・`src/lib/notifications/processPendingNotifications.ts`）とStripe課金（`src/app/(organizer)/plan/stripe-actions.ts`）の呼び出し箇所に`organization.is_demo`分岐を追加し、デモ組織では実送信・実課金を発生させず、画面内に「通知プレビュー」「デモでは課金されません」を表示する。PDF生成（pdfkit）は外部送信を伴わないためそのまま実行してよい。ファイル添付は同梱サンプルのみ選択させ、任意の顧客資料アップロードは受け付けない。

**同時アクセスの扱い（P0の割り切り）**：訪問者ごとの完全なデータ隔離は行わず、共有のデモ組織1つを全訪問者が使う。「最初に戻す」ボタンでdemo org内データのみ即時再シードし、加えて深夜バッチ（Supabase pg_cronまたはVercel Cron）で強制リセットする保険をかける。同時訪問が増えて実運用上の問題が出た場合は、訪問者ごとのエフェメラルなdemo org発行（P2、5章末尾参照）へ拡張する。

## 5. TimeRex等との外部予約連携

ユーザーの「timelexなど」はTimeRex等の予定調整サービスを候補として扱う。プロバイダー固定の自作カレンダーは開発しない。

公式ページで予約ウィジェット、Google/Outlookカレンダー、Web会議ツールとの連携が案内されている。採用プランの機能、埋め込み方法、Webhookの利用可否は実装時に公式仕様で確認する。コードやイベント名を推測しない。

### 5.1 段階的な連携

- P0：設定済みのHTTPS予約URLを別タブで開く。新しいタブで外部サービスへ移動することをリンク名・補助文で伝え、noopener noreferrerを設定。
- P1：契約プランで使える公式ウィジェットを相談セクションに設置。「空き日時を確認する」を押した時点でロード。初回表示時に重い外部スクリプトを実行しない。
- 常に予約URLへの代替リンクを残す。読み込み失敗時は「予約画面を別のタブで開く」と問い合わせ先を提示。
- モバイルで使いにくい場合は外部ページ方式を優先。埋め込みが使えなくても予約導線を失わない。

### 5.2 予約側の設定案

相談枠15分、前後の余裕10分、公開枠は担当者の稼働時間、当日直前予約は避ける設定案。基準TZはAsia/Tokyo、予約サービス上の表示タイムゾーンを明示する。

質問：氏名・メールアドレス・会社名を基本とし、次回開催時期・出展者数・相談内容は任意。サービスが必須項目の変更に対応するか確認。広告配信の同意を予約必須条件にしない。

空き枠管理、Google Meet等のURL発行、確認通知、変更・キャンセル、リマインドは外部サービスが担当。テンジポートから重複する確認メールを送らない。

### 5.3 予約完了の扱い

予約ボタンのクリック、ウィジェットの表示、LPへの帰還だけを「予約完了」として記録しない。

公式の完了通知・Webhook等が利用できる場合のみ、文書化された検証方法、重複防止、キャンセル・変更の扱いを実装する。秘密情報はサーバー側に保持。署名等の仕様が存在するか確認し、未確認の署名ヘッダー名・イベント名を作らない。

Webhook不可なら外部サービスの予約一覧からCRMに手動記録し、LP計測はクリックまでに留める。クロスオリジンiframeのDOMを読み取らない。公式postMessageがある場合のみ送信元originとpayloadを検証する。

## 6. デザイン

方向性：清潔感、信頼感、控えめな高級感。白ベース、濃紺、クリアブルー、十分な余白。採用済み01ロゴの形状を変更しない。

色の初期案：本文/navy #102C50、primary #0068C8、cyan #08AEEA、背景 #F5F8FC、境界 #DCE5EF。ロゴ由来の厳密な抽出色ではなく、UI用提案値。シアン上の白文字等はコントラストを検証して調整。

本文16px以上を基本、行高1.7前後、見出しは明確な階層。最大幅1120〜1200px程度。PCはヒーロー2カラム、モバイルは文言→CTA→画面の順。

過度なグラデーション、装飾だけの巨大余白、常時アニメーション、架空の顧客ロゴ帯を避ける。カードを並べるだけにせず、実画面で具体性を出す。

WCAG 2.2 AAを目標にコントラスト、キーボード、フォーカス、見出し順、フォームラベルを確認。ボタンは44px程度の操作領域を確保。動画に字幕、画像に意味のある代替テキスト。状態は色だけで区別しない。

## 7. 設定と実装構造

次の意味を持つ設定を既存構成に合わせて用意する。名称は例であり環境変数の特定接頭辞を強制しない。

| 設定 | 用途 |
|---|---|
| demoAppPath | 操作デモの入口 |
| signupUrl / signupEnabled | 実サービスの利用開始先と公開可否 |
| documentViewUrl / documentDownloadUrl | 営業資料の閲覧・取得 |
| bookingProvider / bookingUrl | 外部予約先 |
| bookingEmbedEnabled | 公式ウィジェットを使用するか |
| contactEmail | 実際に受信できる問い合わせ先 |
| pricingApproved / annualPriceApproved | 料金公開の承認状態（2026-09-13時点でいずれも確定・承認済み。`docs/open-decisions.md`参照） |
| annualPrice / taxLabel | 年額と税表示 |
| videoUrl / videoPoster | 実動画・ポスター |
| analyticsEnabled | 分析設定 |

公開URLと秘密鍵を分ける。管理画面から任意スクリプト文字列を受け取って実行する設計にしない。許可済み予約プロバイダーの公式コードを実装する。ユーザー指定URLへの無制限リダイレクトを作らない。

UTMはutm_source、utm_medium、utm_campaign、utm_contentの許可項目のみ取り扱う。氏名・メールをURLや分析イベントに含めない。外部予約へのパラメータ付与はプロバイダーが対応する場合だけ行う。

## 8. 計測仕様

| イベント | 発火条件 |
|---|---|
| demo_lp_view | LPの表示 |
| demo_start_click | デモ入口のクリック |
| demo_started | 操作画面が実際に初期化完了 |
| demo_step_completed | 各ガイド操作の完了。step_id付き |
| demo_completed | 主要体験フローの完了 |
| document_view_click | 資料閲覧のクリック |
| document_download_click | PDF取得操作。取得成功・読了を意味しない |
| booking_open_click | 予約開始操作 |
| booking_confirmed | 検証済み公式完了イベント、または外部予約記録との照合 |
| signup_click | 利用開始のクリック |
| signup_completed | 実システムで登録成功した事実 |
| paid_conversion | 実システムで決済成功した事実。デモから発火不可 |

基本属性：campaign、variant、cta_location、device_category、必要最小限の匿名セッションID。自由入力・会社名・メールアドレスを一般分析基盤に送らない。

デモの再描画で同じ完了イベントを大量送信しない。匿名訪問者を特定の主催会社に推測で紐づけない。利用開始等で正当に識別できた段階のみ、必要な範囲でキャンペーン情報を連携。

分析拒否・広告ブロック・外部ウィジェット停止でも、デモ・資料・予約へのリンクは利用できるようにする。プライバシー方針と採用ツールに合わせて同意制御を実装する。

## 9. 性能・SEO

LPは主要文言をHTMLで取得できる構成。title案「登録不要デモ｜展示会・出展者管理クラウド テンジポート」。descriptionは機能と登録不要の操作体験を簡潔に説明。OGは実ロゴ・実画面を使用する。

公開LPは本番ドメインのcanonicalを設定。ステージングと操作デモはnoindex。ただしnoindexをアクセス制御とみなさない。

動画、外部予約、デモ本体は遅延読み込み。画像の幅高さを指定。目標はLCP 2.5秒以内、CLS 0.1以下、INP 200ms以内。開発段階はラボ計測、公開後は取得可能な実測で確認し、未測定を達成済みとしない。

## 10. 受け入れ条件

1. 375px、768px、1440pxで主要文言・CTA・料金・資料・予約が読みやすく、LPに横スクロールがない。
2. 主CTAから登録なしでデモが起動する。
3. ガイドを閉じても自由操作でき、リセットで全初期状態に戻る。
4. 公開・出展者確認・入金確認の各操作が正しい対象と集計へ反映される。
5. 資料閲覧だけでは確認済みにならず、旧版の確認状態を新版へ引き継がない。
6. デモから本番API・メール・決済への要求が発生しない。別ブラウザ間で状態が混ざらない。
7. 資料の閲覧とダウンロードが実ファイルへつながり、内容・料金がLPと一致する。
8. 外部予約URLが正しく、埋め込みを無効にしても別タブ予約・問い合わせが可能。
9. 予約クリックを予約完了として数えない。サンプル操作を受注として数えない。
10. 外部サービスで予約・変更・キャンセルを検証する場合は、管理されたテスト枠と明示された実行権限のもとで実施する。
11. キーボード操作、フォーカス、コントラスト、字幕、ラベルを確認。
12. 料金計算の30社・31社・300社と年間上限を検証。
13. コンソールエラー・壊れたリンク・架空の実績・未確定価格の公開がない。

機能テストは料金境界、対象別状態、デモ分離、予約完了の誤計測防止を優先。見出し文字列をなぞるだけのテストは増やさない。

**テスト基盤**：本プロジェクトに既存のテスト基盤がないため、Playwrightを導入する。用途は主に(1) 4.2節の体験シナリオ全ステップの自動E2E（未提出3社の絞り込み→資料公開→出展者側「確認しました」→主催者側の反映→入金確認→CSV出力）、(2) デモ組織からResend/Stripeへの実呼び出しが発生しないことのネットワークレベル検証（該当APIをモック/インターセプトし呼び出しゼロを確認）、(3) 料金シミュレーターの境界値（30/31/300社、年間上限）、(4) 375/768/1440pxのレスポンシブと基本的なアクセシビリティ（キーボード操作・フォーカス可視性）。E2Eの実行対象はデモ専用組織に限定し、本番組織・本番課金には触れない。axe-core等のa11y監査ツールの併用も検討する。

## 11. 公開前に埋める事項

2026-09-13時点で確定済み（`docs/open-decisions.md`参照。以下は完了扱い）：本番ドメイン（tenjiport.com）、料金（開催ごと・年間とも）、特定商取引法に基づく表記（`/legal/tokushoho`）、採用済みロゴ（`public/tenjiport_logo.png`）。

引き続き未確定・未着手：

- 実サービス利用開始URL（`signupUrl`）の公開可否。既存の主催者マジックリンク不具合（カスタムSMTP対応待ち、`docs/handoff.md`参照）が残る間は、セルフサービス開始の訴求（3.5節「会議なしでも利用開始できます」）を掲載しない。
- 営業PDF資料は未準備のため3.7節の「準備中」表示で運用する。操作動画はPlaywrightで実UI・実データを録画して用意済み（`scripts/record-demo-video.mjs`、字幕はvtt、ffmpeg未導入のためwebm形式）。
- TimeRex等の契約プラン、予約URL、担当カレンダー、会議ツール、受付時間、予約質問。未契約の間は5.1節P0（外部URL直リンク）も使えないため、予約セクションは「メールでご相談ください」のフォールバックで運用する。
- 分析ツール（GA4等）の選定・導入状況。プライバシーポリシー・利用規約への実リンク。
- DMARCレコード（`_dmarc.tenjiport.com`）未設定（`docs/handoff.md`記載、Yahoo!メール等への到達率に影響の可能性）。

未提供の項目があっても開発は進められる。公開に必要な情報が揃うまでは本番公開・予約受付開始を完了扱いにしない。

## 12. Claude Codeの納品報告

変更したページ・主要コンポーネント、設定方法、実装済みと未対応、確認した端末幅、検証結果、公開前に必要な設定を短く報告。実装が整った状態のレビュー用URLまたは起動手順を提示する。

## 13. 外部連携の参照先

2026-09-12確認。機能・プランは実装時に再確認する。

- [TimeRex公式](https://timerex.net/)：カレンダー・会議ツール連携等。
- [TimeRex予約ウィジェット](https://timerex.net/feature/productivity/widget)：公式埋め込み機能。
- [TimeRex開発者向け情報](https://developers.timerex.net/)：API等を実装する場合の確認先。今回、個別エンドポイントやWebhook仕様は未検証。

外部ツールの実績・認証・効果の数値をテンジポート自身の実績として転用しない。
