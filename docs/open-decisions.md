# 残る判断事項

作成日：2026-09-08

設計・実装は以下が未決のままでも進められる。仮値を確定条件として扱わず、本番課金・本番公開の前に確定する（ブリーフ14章 + 本計画でのブラッシュアップ追加分）。

| 項目 | 現状 |
|---|---|
| サービス名・ドメイン | 未定 |
| 通常プランの最終価格・税処理 | 9,800円＋31社目以降300円（税別）が現行案。テスト値として`pricing_configs`に投入済み（`is_test=true`） |
| 通常プランの締め日・最終精算 | 月次精算等は設計案。締め日は未確定 |
| 年間プラン | 定額のみ確定。年額・開催上限は未定。1開催300社案はテスト値として`annual_plan_configs`に投入済み（`is_test=true`） |
| 上限超過時・決済失敗時の操作制限 | 要設計。自動超過請求は禁止 |
| 参加取消・重複登録の課金訂正 | 本番課金前に確定。構造としては`usage_ledger`の`correction_credit`/`correction_debit`で対応可能 |
| 開催利用期間・保存期間・ファイル容量 | 未定。設定値化する |
| 複数担当者・権限・グループの詳細 | 最小構成（owner/admin/staff、owner/staff）を実装、詳細は運用しながら調整 |
| 実際の入力項目と出力様式 | 既存PDF・Excelが入手でき次第具体化 |
| インボイス制度（適格請求書）対応 | `ServiceInvoice`（利用料請求）への適格請求書発行事業者登録番号の記載要否が未検討。消費税の扱いと合わせて確定が必要（ブラッシュアップ提案3） |
| 特定商取引法に基づく表記の記載内容 | ページ自体は`/legal/tokushoho`に作成済み。事業者名・所在地・連絡先等【要入力】箇所を、ユーザー側（BlackishGear）で正式な情報に差し替える必要がある（ブラッシュアップ提案4） |
| Stripe自動課金・Webhook | カード登録（Checkout, mode=setup）は実装・動作確認済み。確定した請求書（ServiceInvoice）への実際のカード課金（off-session PaymentIntent）と、Webhookのダッシュボード登録（署名検証・冪等性の受信基盤のみ実装済み）は未対応。本番URLが決まり次第、Webhookエンドポイント登録と自動課金処理を実装する |
| 決済プロバイダの最終選定 | Stripeを推奨として実装を進めるが、Komoju/PAY.JP等の国内事業者との比較は本番反映前に実施 |
| Phase 2の未実装項目 | ①CAPTCHA（Cloudflare Turnstile等）は`events.spam_guard_config`に受け皿はあるが未接続。外部サービスのサイトキー取得が必要なため、乱用が実際に発生してから導入する方針（レート制限・ハニーポットは実装済み） ②ファイル添付・繰り返し入力タイプのフィールドは「近日対応」表示のみで実未対応（Phase 3のSupabase Storage連携時にまとめて実装予定） ③1ユーザー1ブランド限定（同一メールで複数ブランドを使い分けるUIは未実装、`start_or_resume_submission`は常に最初のブランドを再利用） |
| Supabaseプロジェクト | アカウントは別プロジェクト（広告健康診断SaaS）で作成済み。本SaaS用には新規プロジェクト作成を推奨（テナント・請求・障害影響範囲の分離のため）。作成後、URL/anon key/service role keyを`.env.local`へ設定（チャット等には貼らないこと） |
| 主催者側のメール確認 | 開発中はSupabase Authの Providers > Email > Confirm email をOFFにして動作確認している（カスタムSMTP未設定のため）。本番公開前にONへ戻し、確認メールリンクのテンプレート設定（`/auth/confirm`形式）とSMTP（Resend等）を設定すること |

未決事項があっても画面・データ・テスト環境の設計は進められる。
